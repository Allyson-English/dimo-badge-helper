{
  "name": "dimo-badge-garage",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && git add dist && git commit -m 'Build for GitHub Pages' && git subtree push --prefix dist origin gh-pages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^22.10.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.7"
  },
  "dependencies": {
    "ethers": "^6.13.5"
  },
  "vite": {
    "build": {
      "outDir": "dist",
      "assetsDir": "assets",
      "rollupOptions": {
        "input": {
          "main": "./index.html"
        }
      }
    }
  }
}
